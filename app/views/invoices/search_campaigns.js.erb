var campaigns_list = $("#invoice_select_campaigns_list");

campaigns_list.empty();
campaigns_list.append("<%= escape_javascript(render partial: 'campaigns/partials/select_list', locals: {campaigns: @campaigns, url: invoices_path, method: :post}).html_safe %>");
