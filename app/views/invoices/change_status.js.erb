var aasm_status = $("#invoice_<%= @invoice.id %>_status");

aasm_status.empty();
aasm_status.append("<%= escape_javascript(render partial: 'invoices/partials/status', locals: {invoice: @invoice}).html_safe %>");
